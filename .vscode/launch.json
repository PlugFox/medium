{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "server",
            "request": "launch",
            "type": "dart",
            "program": "bin/server.dart",
            "cwd": "${workspaceRoot}",
            "env": {
                "DATA": "data"
            }
        }
    ]
}